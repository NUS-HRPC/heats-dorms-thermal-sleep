pandas==2.3.0
numpy==2.2.6
pyarrow==23.0.0
scipy==1.16.0
statsmodels==0.14.5
plotly==6.2.0
pythermalcomfort==3.9.2
patsy==1.0.1
openpyxl==3.1.5
matplotlib==3.10.6
kaleido==1.3.0
ipykernel==6.29.5

# kaleido (used by plotly's fig.write_image calls, currently all commented
# out as optional 600 dpi TIFF exports) needs a Chrome/Chromium binary on
# PATH. If write_image raises a "Kaleido requires Chrome" error, run
# `plotly_get_chrome` once, or point kaleido at an existing installation.
